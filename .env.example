# Census Bureau API key (free: https://api.census.gov/data/key_signup.html)
CENSUS_API_KEY=

# HUD USPS Crosswalk API token (free: https://www.huduser.gov/portal/dataset/uspszip-api.html)
HUD_API_TOKEN=

# OpenRouteService API key for isochrones (free: https://openrouteservice.org/dev/#/signup)
ORS_API_KEY=

# OSRM base URL (optional, defaults to public demo server)
# For self-hosted: http://localhost:5000
OSRM_BASE_URL=http://router.project-osrm.org

# SEC EDGAR User-Agent (REQUIRED — server will refuse to start without it)
# Format: "YourAppName your-real-email@domain.com"
# SEC blocks requests with generic/placeholder User-Agents.
# See: https://www.sec.gov/os/accessing-edgar-data
SEC_USER_AGENT=

# Bureau of Labor Statistics API key (free: https://www.bls.gov/developers/home.htm)
BLS_API_KEY=

# Optional local path to a normalized ACGME Program Search export.
# Generate it with:
#   python3 scripts/import_acgme_programs.py /path/to/acgme-program-search-export.csv
ACGME_PROGRAMS_CSV=

# Optional local path to a downloaded CareSet DocGraph CSV used by
# physician-referral-network. The server can import it through the
# load_docgraph_cache MCP tool.
DOCGRAPH_CSV_PATH=

# SAM.gov API key (free: https://sam.gov/content/entity-registration)
SAM_GOV_API_KEY=

# ONC CHPL API key (free: https://chpl.healthit.gov/#/resources/api)
CHPL_API_KEY=

# Google Custom Search API key (free tier available)
GOOGLE_CSE_API_KEY=

# Google Custom Search Engine ID
GOOGLE_CSE_ID=

# Optional per-process Google CSE guardrail. Set to 0 to disable.
GOOGLE_CSE_SESSION_LIMIT=40

# Optional in-memory TTL for identical Google CSE queries (seconds)
GOOGLE_CSE_CACHE_TTL_SECONDS=21600

# Proxycurl API key (paid, optional for web-intelligence enrichment)
PROXYCURL_API_KEY=

# Maximum raw MRF download size before the price-transparency server aborts.
# Default 10 GiB.
MRF_MAX_DOWNLOAD_BYTES=10737418240

# Minimum free disk space to preserve while downloading MRF files.
MRF_MIN_FREE_BYTES=2147483648
